import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  basePath: '/dawdmaow/poor_mans_excel',
  images: {
    unoptimized: true,
  },
  /* config options here */
};

export default nextConfig;
